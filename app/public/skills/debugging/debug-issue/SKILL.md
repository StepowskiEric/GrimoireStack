---
name: "debug-issue"
description: "Forces the reproduce → isolate → fix → verify cycle. Graph-powered code navigation traces issues through the system along dataflow edges."
---

# Debug Workflow

> Forces the reproduce → isolate → fix → verify cycle. Graph-powered code navigation traces issues through the system along dataflow edges.

*This skill is part of the GrimoireStack catalog. Full documentation is pending.*
